# TODO

Replace all calls in MultipartFormBuilder with calls to ContentBuilder-methods.
Replace all calls in UrlEncodedFormBuilder with calls to ContentBuilder-methods.
Give XmlSerialization a refactor pass. Maybe turn it into a builder similar to JsonSerialization.

# DONE

Replace all Url Parameter calls in Request with calls to ContentBuilder-methods.
Replace all Template Parameter calls in Request with calls to ContentBuilder-methods.
Replace all Body calls in Request with calls to ContentBuilder-methods.